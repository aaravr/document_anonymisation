# Development / test dependencies. Layered on top of requirements.txt.
-r requirements.txt

pytest>=7.4
pytest-cov>=4.1
ruff>=0.1
mypy>=1.7

# Optional Presidio recognisers, useful for the IDP package's tests in strict mode
presidio-analyzer>=2.2
presidio-anonymizer>=2.2

# Test-only NER backbone (the install also writes the en_core_web_sm model below)
